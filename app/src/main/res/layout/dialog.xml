<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <EditText
            android:id="@+id/name_item"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="name "
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@id/amount_item"
            app:layout_constraintTop_toTopOf="parent" />

        <EditText
            android:id="@+id/amount_item"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:hint="amount"
            android:inputType="number"
            app:layout_constraintLeft_toRightOf="@id/name_item"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/compelete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="proceed"
            app:layout_constraintRight_toRightOf="@id/amount_item"
            app:layout_constraintTop_toBottomOf="@id/amount_item" />

        <TextView
            android:id="@+id/cancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginRight="16dp"
            android:text="cancel"
            app:layout_constraintRight_toLeftOf="@id/compelete"
            app:layout_constraintTop_toBottomOf="@id/amount_item" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>